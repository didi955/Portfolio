export interface Skill {
    name: string;
    logo: string;
    link: string;
}

export interface Experience {
    icon: string;
    title: string;
    position: string;
    date: string;
    description?: string;
}
